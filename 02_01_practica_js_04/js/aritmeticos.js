// Operadores

// Operadores unarios   operando operador
// Operadores binarios operando operador operando 
// Operador ternario (ver más adelante sintaxis)

// Operadores aritméticos

// + y -

let resultado = 6;
let factorCorreccion = 2;

let puntuacion = resultado - factorCorreccion;
console.log(puntuacion);

// El operador de suma en los tipos string concatenas las cadenas de caracteres

let nombre = 'Laura';
let mensaje = 'Hola';

let mensajeFinal = mensaje + ' ' + nombre;
console.log(mensajeFinal);

resultado = 10;
factorCorreccion = '3';

puntuacion = resultado + factorCorreccion;
console.log(puntuacion);

puntuacion = resultado - factorCorreccion;
console.log(puntuacion);

// * y /

let a = 10;
let b = 2;

resultado = a / b;
console.log(resultado);

// Valor NaN (not a number)

a = 'Hello';
resultado = a * b;
console.log(resultado);

// Resto %

let ejemploResto = 13 % 2;
console.log(ejemploResto);

// Incremento ++ (sumar una unidad al valor de la variable)

let c = 1;
console.log(c++); // Postincremento
console.log(c);

let d = 10;
console.log(++d); // Preincremento

// Decremento --

// idem anterior